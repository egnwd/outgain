dependencies:
  pre:
    - gem install 'platform-api'

deployment:
  master:
    branch: master
    owner: egnwd
    commands:
      - tar czvf /tmp/slug.tgz --transform='s#^./#./app/#S' --exclude .git .
      - ./deploy.rb /tmp/slug.tgz
